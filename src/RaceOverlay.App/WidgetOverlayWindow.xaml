<Window x:Class="RaceOverlay.App.WidgetOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:RaceOverlay.Engine.Views;assembly=RaceOverlay.Engine"
        mc:Ignorable="d"
        Title="Widget Overlay"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource RO.BackgroundBrush}"
        Topmost="True"
        AllowsTransparency="False"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False">

    <Grid>
        <!-- Layer 0: Widget Content -->
        <ContentControl x:Name="WidgetContent" />

        <!-- Layer 1: Transparent drag overlay (shown only in drag mode) -->
        <Border x:Name="DragOverlay" Background="Transparent" Visibility="Collapsed" />
    </Grid>
</Window>
