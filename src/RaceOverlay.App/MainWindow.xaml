<Window x:Class="RaceOverlay.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RaceOverlay.App"
        mc:Ignorable="d"
        Title="RaceOverlay - Widget Manager" 
        Height="600" 
        Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel -->
        <Border Grid.Column="0" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <DockPanel LastChildFill="True">
                <Border DockPanel.Dock="Top" Background="#E8E8E8">
                    <TextBlock Text="Available Widgets" FontSize="14" FontWeight="Bold" Foreground="#333333" Margin="12"/>
                </Border>
                <ListBox Name="WidgetListBox" ItemsSource="{Binding AvailableWidgets}" SelectedItem="{Binding SelectedWidget}" Background="#F5F5F5" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="10">
                                <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" Foreground="#333333"/>
                                <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#666666" TextWrapping="Wrap" Margin="0,4,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#D0D0D0" ShowsPreview="True"/>

        <!-- Right Panel -->
        <DockPanel Grid.Column="2" LastChildFill="True">
            <Border DockPanel.Dock="Top" Background="#E8E8E8">
                <TextBlock Text="Configuration" FontSize="14" FontWeight="Bold" Foreground="#333333" Margin="12"/>
            </Border>
            <Border DockPanel.Dock="Top" Background="White" Margin="16">
                <TextBlock Text="No widget selected" Foreground="#999999" FontStyle="Italic"/>
            </Border>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="12" Height="40">
                <Button Command="{Binding AddWidgetCommand}" Content="Add Widget" Margin="4" Background="#4CAF50" Foreground="White" FontSize="12" Cursor="Hand" Padding="12,8"/>
                <Button Command="{Binding RemoveWidgetCommand}" Content="Remove Widget" Margin="4" Background="#F44336" Foreground="White" FontSize="12" Cursor="Hand" Padding="12,8"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
